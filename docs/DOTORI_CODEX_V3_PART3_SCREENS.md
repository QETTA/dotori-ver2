# 도토리 CODEX V3 — Part 3: 화면 조립·UX 패턴·랜딩·빌드

> **⚠️ 아카이브** — 초기 설계. 현재 21개 페이지 (문서 10개 대비 2배+). `docs/ops/MASTER_v1.md` 참조

---

## 12. 인앱 레이아웃

파일: `src/app/(app)/layout.tsx`

```tsx
import { AppProvider } from '@/hooks/use-app-state'
import { ToastProvider } from '@/components/dotori/ToastProvider'
import { BottomTabBar } from '@/components/dotori/BottomTabBar'

export default function AppLayout({ children }: { children: React.ReactNode }) {
  return (
    <AppProvider>
      <ToastProvider>
        <main className="mx-auto max-w-md min-h-dvh pb-20 bg-dotori-50">
          {children}
        </main>
        <BottomTabBar />
      </ToastProvider>
    </AppProvider>
  )
}
```

핵심:
- `max-w-md mx-auto` → 모바일 앱 느낌 유지 (데스크톱에서도 센터링)
- `pb-20` → BottomTabBar 높이만큼 여백
- `min-h-dvh` → dynamic viewport height (모바일 주소창 고려)
- AppProvider, ToastProvider는 인앱 전체 감싸기

---

## 13. 화면별 조립 명세 (8개 인앱 + 1개 온보딩 + 1개 랜딩)

### 13-A. 홈 (`src/app/(app)/page.tsx`)

```
'use client'
의존: ActionCard, FacilityCard(compact), SourceChip, Skeleton
훅:  useAppState

┌──────────────────────────────┐
│ 인사 헤더                      │
│ "서연맘, 오늘 관심 시설에 변동이 있어요" │
│ 또는 (온보딩 미완료 시):          │
│ "안녕하세요! 아이 정보를 등록하면    │
│  맞춤 전략을 받을 수 있어요"       │
├──────────────────────────────┤
│ NBA 섹션 (조건부)               │
│ ┌─ ActionCard (high) ────────┐│
│ │ 아이 등록하면 맞춤 전략 받아요  ││
│ └────────────────────────────┘│
│ ┌─ ActionCard (normal) ──────┐│
│ │ 실시간 빈자리 알림 설정       ││
│ └────────────────────────────┘│
│ (조건: dismissedNBAs에 없고,    │
│  condition 평가 결과 true인 것만) │
├──────────────────────────────┤
│ 오늘의 변동 (관심 시설)           │
│ <Heading> "관심 시설 변동"       │
│ FacilityCard compact × 최대3개  │
│ (interests에 해당하는 시설만)     │
│ 관심시설 없으면 이 섹션 숨김       │
├──────────────────────────────┤
│ 서비스 요약 (2×2 그리드)          │
│ ┌──────┬──────┐              │
│ │ 12,847│ 실시간 │              │
│ │ 시설   │ TO 알림│              │
│ ├──────┼──────┤              │
│ │ 24시간 │ 무료   │              │
│ │ AI분석 │      │              │
│ └──────┴──────┘              │
│ (Oatmeal stats-four-columns    │
│  패턴 참고하여 재작성)            │
├──────────────────────────────┤
│ AI 브리핑 카드                   │
│ bg-dotori-100 rounded-xl p-4   │
│ "🌰 오늘의 AI 브리핑"            │
│ "역삼동 반경 1km 내 TO 2건 발생"  │
│ "해피어린이집 대기순번 3번 앞당겨짐" │
│ SourceChip(아이사랑, realtime)   │
│ [자세히 보기 →] → /chat 이동     │
├──────────────────────────────┤
│ 긴급 TO 알림 (조건부)            │
│ FacilityCard(full) 1개          │
│ (status=available && 관심지역)   │
└──────────────────────────────┘

조건부 렌더링 로직:
- user.onboardingCompleted === false → NBA "아이 등록" 최상단 고정
- user.interests.length === 0 → "관심 시설 변동" 섹션 숨김, NBA "동네 추천" 표시
- 오늘 변동 없으면 → "변동 없음, AI가 계속 모니터링 중이에요" 텍스트
```

### 13-B. 토리챗 (`src/app/(app)/chat/page.tsx`)

```
'use client'
의존: ChatBubble, FilterChip, FacilityCard, MapEmbed, CompareTable,
     ActionConfirmSheet, StreamingIndicator, Skeleton
훅:  useAppState, useState, useRef, useEffect

┌──────────────────────────────┐
│ 헤더 (sticky top-0)            │
│ "토리챗" + Badge(Streaming/연결됨)│
│ 우측: 채팅 기록 아이콘            │
├──────────────────────────────┤
│ 프롬프트 칩 바 (스크롤)           │
│ FilterChip: "동네 추천" "입소 전략"│
│ "시설 비교" "서류 준비" "TO 알림"  │
│ (선택 시 → 해당 프롬프트 자동 전송) │
├──────────────────────────────┤
│ 채팅 메시지 영역 (flex-1 overflow)│
│                               │
│ [assistant] 안녕하세요, 서연맘!   │
│   무엇을 도와드릴까요?            │
│   SourceChip(AI분석, realtime)  │
│                               │
│ [user] 우리 동네 어린이집 추천해줘 │
│                               │
│ [assistant] 역삼동 도보 10분 내   │
│   3곳을 찾았어요                 │
│   SourceChip(아이사랑) SourceChip(지도)│
│   ┌─ MapEmbed ──────────────┐ │
│   │ 📍 3곳 지도              │ │
│   └─────────────────────────┘ │
│   ┌─ FacilityCard(compact)──┐ │
│   │ 해피어린이집 · TO 2명    │ │
│   ├─────────────────────────┤ │
│   │ 사랑어린이집 · 대기 3번   │ │
│   ├─────────────────────────┤ │
│   │ 별빛어린이집 · 대기 12번  │ │
│   └─────────────────────────┘ │
│   [비교하기] [관심 등록] [대기 신청]│
│                               │
│ [user] 해피어린이집 대기 신청해줘  │
│                               │
│ [assistant] 대기 신청을 진행할게요│
│   → ActionConfirmSheet 열림     │
│                               │
├──────────────────────────────┤
│ 입력바 (sticky bottom)          │
│ ┌──────────────────────┬────┐│
│ │ 토리에게 물어보세요...    │ ➤ ││
│ └──────────────────────┴────┘│
│ padding-bottom: safe-area      │
└──────────────────────────────┘

Read → UI Card → Confirm → Write 패턴 (핵심 UX):

1. Read: 사용자 "추천해줘" → 커넥터 호출 (아이사랑+지도+프로필)
2. UI Card: 응답에 MapEmbed + FacilityCard 인라인 렌더링
3. Confirm: "대기 신청해줘" → ActionConfirmSheet 바텀시트 올라옴
   - 프리뷰: 시설명, 아이이름, 생년월일
   - 체크박스 동의 → 확인 버튼
4. Write: executing → success → Toast("대기 신청 완료 · 되돌리기")

ActionConfirmSheet 플로우 코드:
  const [sheetOpen, setSheetOpen] = useState(false)
  const [actionStatus, setActionStatus] = useState<ActionStatus>('idle')

  async function handleConfirm() {
    setActionStatus('executing')
    try {
      await fakeApiCall()           // Phase 2에서 실제 API
      setActionStatus('success')
      addToast({ type: 'success', message: '대기 신청이 완료되었어요',
                 action: { label: '되돌리기', onClick: handleUndo } })
      setTimeout(() => setSheetOpen(false), 1500)
    } catch (e) {
      setActionStatus('error')
    }
  }
```

### 13-C. 탐색 (`src/app/(app)/explore/page.tsx`)

```
'use client'
의존: catalyst/input, FilterChip, FacilityCard, Skeleton, EmptyState
훅:  useState

┌──────────────────────────────┐
│ 검색바                         │
│ <Input> placeholder="시설 이름, │
│   지역 검색"                    │
│ 돋보기 아이콘 좌측               │
├──────────────────────────────┤
│ 필터 칩 행 (수평 스크롤)         │
│ FilterChip: "전체" "국공립" "민간"│
│ "가정" "직장" "TO있음" "연장보육" │
├──────────────────────────────┤
│ 탭: "주변" | "인기" | "최근본"   │
├──────────────────────────────┤
│ (탭=주변) 시설 리스트            │
│ FacilityCard(compact) 반복      │
│ (검색/필터 결과가 없으면)         │
│ <EmptyState                    │
│   title="검색 결과가 없어요"     │
│   description="다른 조건으로 검색해보세요"│
│   actionLabel="토리챗에서 추천받기"│
│   actionHref="/chat?prompt=추천" │
│ />                             │
├──────────────────────────────┤
│ 토리챗 딥링크 CTA                │
│ bg-dotori-100 rounded-xl p-4   │
│ "원하는 조건이 복잡하다면?"       │
│ [토리챗에서 AI 추천받기 →]       │
└──────────────────────────────┘
```

### 13-D. 이웃 (`src/app/(app)/community/page.tsx`)

```
'use client'
의존: FilterChip, EmptyState, catalyst/avatar, catalyst/badge, catalyst/button
훅:  useState

┌──────────────────────────────┐
│ GPS 인증 배너 (조건부)           │
│ (gpsVerified=false 일 때만)     │
│ "동네 인증하고 이웃과 소통하세요"  │
│ [인증하기] 버튼                  │
│ bg-forest-50 rounded-xl p-3    │
├──────────────────────────────┤
│ 탭: "최신" | "인기" | "질문" |   │
│     "후기" | "정보"              │
├──────────────────────────────┤
│ (게시글 있을 때)                 │
│ 피드 아이템 반복:                │
│ ┌────────────────────────────┐│
│ │ Avatar + 역삼맘 + ✓인증      ││
│ │ 4시간전                      ││
│ │                              ││
│ │ "해피어린이집 혹시 올해 TO..."  ││
│ │                              ││
│ │ [해피어린이집] ← facilityTag  ││
│ │                              ││
│ │ ▼ AI 요약 보기 (접이식)       ││
│ │ "AI: 해피어린이집의 올해..."   ││
│ │                              ││
│ │ ♡ 5  💬 3                    ││
│ └────────────────────────────┘│
│                               │
│ (게시글 없을 때)                │
│ <EmptyState                    │
│   icon={<UserGroupIcon />}     │
│   title="아직 이웃 글이 없어요"  │
│   description="첫 글을 작성해보세요"│
│   actionLabel="글 작성하기"      │
│ />                             │
├──────────────────────────────┤
│ FAB (우하단)                    │
│ + 아이콘, 글쓰기 버튼            │
│ bg-dotori-500 text-white       │
│ rounded-full w-14 h-14 shadow-lg│
│ fixed bottom-24 right-4        │
└──────────────────────────────┘
```

### 13-E. MY (`src/app/(app)/my/page.tsx`)

```
'use client'
의존: catalyst/avatar, catalyst/badge, catalyst/button, catalyst/description-list,
     ActionCard, catalyst/switch
훅:  useAppState

┌──────────────────────────────┐
│ 프로필 헤더                     │
│ Avatar(large) + 서연맘          │
│ Badge: "무료 플랜"              │
│ 서울 강남구 역삼동               │
├──────────────────────────────┤
│ 요약 카드 (2×2 그리드)           │
│ ┌──────────┬──────────┐      │
│ │ 관심시설 2 │ 대기신청 1│      │
│ ├──────────┼──────────┤      │
│ │ 알림설정 0 │ 상담이력 5│      │
│ └──────────┴──────────┘      │
├──────────────────────────────┤
│ 내 아이 섹션                    │
│ ┌────────────────────────────┐│
│ │ 서연 · 2024.03 · 여아       ││
│ │ [정보 수정] 버튼              ││
│ └────────────────────────────┘│
│ (아이 미등록 시):                │
│ <ActionCard                    │
│   priority="high"              │
│   title="아이 등록하면 맞춤 전략" │
│   actionLabel="등록하기"        │
│ />                             │
├──────────────────────────────┤
│ 메뉴 리스트                     │
│ 알림 설정           >           │
│ 플랜 관리           >           │
│ 공지사항            >           │
│ 이용약관            >           │
│ 고객센터            >           │
│ 앱 버전 1.0.0                  │
├──────────────────────────────┤
│ 로그아웃 버튼                   │
│ Button(plain) text-red-500     │
└──────────────────────────────┘
```

### 13-F. 시설 상세 (`src/app/(app)/facility/[id]/page.tsx`)

```
'use client'
의존: FacilityCard(full), SourceChip, MapEmbed, CompareTable,
     ActionConfirmSheet, catalyst/badge, catalyst/button, catalyst/description-list
훅:  useAppState, useParams, useState

┌──────────────────────────────┐
│ 뒤로가기 + 시설명 + 공유 아이콘   │
├──────────────────────────────┤
│ 시설 사진 갤러리 (수평 스크롤)    │
│ 사진 없으면 placeholder 이미지   │
├──────────────────────────────┤
│ 기본 정보                       │
│ 시설명 (큰 글씨)                │
│ Badge(시설유형) + Badge(상태)   │
│ SourceChip(아이사랑, 최종 업데이트)│
├──────────────────────────────┤
│ 정원 현황 (그리드)               │
│ ┌───────┬───────┬───────┐    │
│ │ 정원   │ 현원   │ 대기   │    │
│ │ 80    │ 72    │ 0     │    │
│ └───────┴───────┴───────┘    │
├──────────────────────────────┤
│ 위치 & 교통                     │
│ MapEmbed(단일 시설)             │
│ 주소: 서울 강남구 역삼동 123      │
│ 전화: 02-555-1234              │
│ 도보: 5분 (내 위치 기준)         │
├──────────────────────────────┤
│ 특징 & 프로그램                  │
│ FilterChip(비선택형): 통학버스,   │
│ 연장보육, 급식자체조리            │
├──────────────────────────────┤
│ 이웃 후기                       │
│ (커뮤니티에서 이 시설 태그된 글)   │
│ 없으면 "아직 후기가 없어요"       │
│ [후기 작성하기] 버튼              │
├──────────────────────────────┤
│ AI 분석 카드                    │
│ bg-dotori-100 rounded-xl p-4   │
│ "🌰 토리의 분석"                │
│ "이 시설은 3월 입소 가능성이 높아요"│
│ "올해 평균 TO 발생 주기: 2.3개월" │
│ SourceChip(AI분석, realtime)    │
├──────────────────────────────┤
│ 액션 바 (sticky bottom)         │
│ ┌──────────┬──────────────┐  │
│ │ 관심 등록  │ 대기 신청하기 │  │
│ │ (outline) │ (solid dotori)│  │
│ └──────────┴──────────────┘  │
│ → 클릭 시 ActionConfirmSheet   │
└──────────────────────────────┘
```

### 13-G. 온보딩 (`src/app/(onboarding)/page.tsx`)

```
'use client'
의존: ProgressStepper, catalyst/input, catalyst/select, catalyst/radio,
     catalyst/checkbox, catalyst/button, FilterChip
훅:  useState

┌──────────────────────────────┐
│ ProgressStepper (3단계)        │
│ [아이 정보] ─── [지역] ─── [관심] │
├──────────────────────────────┤
│ Step 1: 아이 정보               │
│ Input: 아이 이름                │
│ Input: 생년월일 (YYYY-MM)       │
│ Radio: 성별 (여아/남아/선택안함)  │
│                               │
│ [다음] 버튼                     │
├──────────────────────────────┤
│ Step 2: 거주 지역               │
│ Select: 시/도                  │
│ Select: 시/군/구 (시도 선택 후)  │
│ Input: 동/읍/면 (선택)          │
│                               │
│ [이전] [다음] 버튼              │
├──────────────────────────────┤
│ Step 3: 관심 유형               │
│ FilterChip 다중선택:            │
│ "국공립" "민간" "가정" "직장"     │
│ "통학버스" "연장보육" "급식자체조리"│
│ "영아전문" "장애아통합"           │
│                               │
│ [이전] [시작하기] 버튼           │
├──────────────────────────────┤
│ 완료 후:                        │
│ router.push('/') 홈으로 이동     │
│ dispatch SET_USER (onboarding=true)│
│ addToast({ type: 'success',     │
│   message: '맞춤 설정 완료! 토리가 분석을 시작할게요'})│
└──────────────────────────────┘

건너뛰기 링크: 각 스텝 상단 우측 "건너뛰기" 텍스트 버튼
  → 기본값으로 저장하고 홈 이동
```

---

## 14. 랜딩 페이지

### 14-A. 레이아웃 (`src/app/(landing)/layout.tsx`)

```tsx
export default function LandingLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
```

별도 layout 사용하여 BottomTabBar가 표시되지 않게 한다.

### 14-B. 랜딩 페이지 (`src/app/(landing)/landing/page.tsx`)

Pocket 템플릿 기반. 아래 순서로 섹션 조립:

```
┌──────────────────────────────────────┐
│ 1. Header (Pocket Header.tsx 커스텀)   │
│    로고: 도토리 🌰                     │
│    NavLinks: 기능, 후기, 가격, FAQ     │
│    CTA: "시작하기" → /onboarding      │
├──────────────────────────────────────┤
│ 2. Hero (Pocket Hero.tsx 커스텀)       │
│    헤드라인: "우리 아이 어린이집,        │
│              AI가 찾아드려요"           │
│    서브: "12,000+ 시설 데이터 기반      │
│          실시간 입소 전략"              │
│    CTA: 앱스토어 버튼 (향후)           │
│    PhoneFrame: 도토리 앱 스크린샷       │
├──────────────────────────────────────┤
│ 3. Stats (Oatmeal stats 패턴 재작성)   │
│    "12,847" 전국 시설  |  "93.8%" 시간절감│
│    "24h" AI 모니터링  |  "무료" 기본요금  │
│    Wallpaper(warm) 배경               │
├──────────────────────────────────────┤
│ 4. Features 1 (Pocket PrimaryFeatures)│
│    탭 3개:                            │
│    "실시간 빈자리" → 폰 스크린(홈 화면) │
│    "AI 맞춤 전략" → 폰 스크린(토리챗)  │
│    "동네 이웃 커뮤니티" → 폰 스크린(이웃)│
├──────────────────────────────────────┤
│ 5. Features 2 (Pocket SecondaryFeatures)│
│    아이콘 그리드:                       │
│    TO 알림 | 서류 체크리스트 |           │
│    시설 비교 | 보육료 지원 가이드         │
├──────────────────────────────────────┤
│ 6. Testimonials (Salient 커스텀)       │
│    육아맘 후기 3~5개                   │
│    "토리 덕분에 국공립 입소 성공했어요"   │
├──────────────────────────────────────┤
│ 7. Pricing (Salient 커스텀)            │
│    무료 플랜 vs 프리미엄 플랜            │
│    무료: 시설 검색, 기본 AI 분석         │
│    프리미엄: 실시간 알림, 비교 리포트,    │
│             우선 대기 신청               │
├──────────────────────────────────────┤
│ 8. FAQ (도토리 FAQ 컴포넌트)            │
│    5~7개 질문/답변                     │
│    "정말 무료인가요?"                   │
│    "어떤 데이터를 사용하나요?"           │
│    "대기 신청은 어떻게 하나요?"          │
├──────────────────────────────────────┤
│ 9. CTA (Pocket CallToAction 커스텀)    │
│    "지금 시작하세요"                    │
│    Button → /onboarding               │
│    Wallpaper(green) 배경               │
├──────────────────────────────────────┤
│ 10. Footer (Pocket Footer 커스텀)      │
│     로고 + 링크 + © 2026 도토리         │
└──────────────────────────────────────┘

⚠️ 랜딩 수정 시 주의사항:
- Pocket의 AppStoreLink → 도토리는 "시작하기" 버튼으로 교체
- 모든 영문 → 한국어
- Pocket 이미지 → 도토리 앱 스크린샷 (public/images/)
- Salient 폰트 Lexend → 삭제 (Pretendard 통일)
- Salient Container → Pocket Container 또는 직접 작성으로 통일
```

---

## 15. Read → UI Card → Confirm → Write 패턴 구현 가이드

이것이 도토리의 **핵심 UX 패러다임**이다. ChatGPT Apps의 MCP 패턴을 육아 도메인에 역방향 번역한 것.

### 15-1. 패턴 개요

```
[사용자 질문]
     ↓
[토리 내부: 커넥터 호출]
  ├─ 아이사랑 커넥터 (시설 데이터)
  ├─ 지도 커넥터 (거리/위치)
  ├─ 프로필 커넥터 (아이 나이, 선호)
  └─ AI 엔진 (종합 분석)
     ↓
[Read 결과 → UI Card 렌더링]
  ChatBubble 안에 인라인:
  - FacilityCard (시설 정보)
  - MapEmbed (지도)
  - CompareTable (비교)
  - 차트 (확률 변동, Phase 2)
     ↓
[사용자 Write 요청: "대기 신청해줘"]
     ↓
[Confirm: ActionConfirmSheet 표시]
  - 프리뷰 (시설명, 아이 정보)
  - 체크박스 동의
  - 확인 버튼
     ↓
[Write: 실행]
  - ActionStatus: executing → success/error
  - Toast 표시 (되돌리기 포함)
```

### 15-2. 구현할 액션 목록 (MVP)

| 액션 | 트리거 | 프리뷰 | 실행 결과 | Toast |
|------|--------|--------|----------|-------|
| 관심 등록 | "관심 등록" 버튼 | 시설명 | interests에 추가 | "관심 시설에 추가했어요 · 되돌리기" |
| 대기 신청 | "대기 신청" 버튼 | 시설명, 아이, 생년월일 | 대기 신청 기록 | "대기 신청 완료 · 되돌리기" |
| 알림 설정 | "알림 받기" 버튼 | 시설명, 조건 | 알림 등록 | "알림이 설정됐어요" |
| 비교 리포트 | "비교하기" 버튼 | 시설 2~3개 | CompareTable 렌더링 | 토스트 불필요 |
| 서류 체크리스트 | "서류 준비" 버튼 | 시설유형, 아이나이 | 체크리스트 카드 | 토스트 불필요 |

### 15-3. 토리챗 메시지 렌더링 분기

```typescript
// ChatBubble 내부에서 EmbeddedCard를 렌더링하는 함수
function renderEmbeddedCard(card: EmbeddedCard) {
  switch (card.type) {
    case 'facility':
      return <FacilityCard facility={card.data as Facility} compact />
    case 'map':
      return <MapEmbed facilities={card.data as MapFacility[]} />
    case 'compare':
      return <CompareTable facilities={card.data as Facility[]} highlightBest />
    case 'checklist':
      return <ChecklistCard items={card.data as ChecklistItem[]} />
    case 'chart':
      return <div>Phase 2: 확률 변동 차트</div>
    default:
      return null
  }
}
```

---

## 16. 디렉토리 구조 최종

```
dotori-app/
├── public/
│   ├── images/
│   │   ├── phone-frame.svg        (Pocket)
│   │   ├── dotori-logo.svg        (직접 제작)
│   │   ├── dotori-avatar.png      (토리 AI 아바타)
│   │   ├── icon-192.png           (PWA)
│   │   └── icon-512.png           (PWA)
│   └── manifest.json
├── src/
│   ├── app/
│   │   ├── globals.css            (테마 시스템)
│   │   ├── layout.tsx             (루트 레이아웃)
│   │   ├── (app)/
│   │   │   ├── layout.tsx         (인앱 레이아웃: TabBar+Provider)
│   │   │   ├── page.tsx           (홈)
│   │   │   ├── explore/
│   │   │   │   └── page.tsx       (탐색)
│   │   │   ├── chat/
│   │   │   │   └── page.tsx       (토리챗)
│   │   │   ├── community/
│   │   │   │   └── page.tsx       (이웃)
│   │   │   ├── my/
│   │   │   │   ├── page.tsx       (MY)
│   │   │   │   └── settings/
│   │   │   │       └── page.tsx   (설정)
│   │   │   └── facility/
│   │   │       └── [id]/
│   │   │           └── page.tsx   (시설 상세)
│   │   ├── (onboarding)/
│   │   │   └── page.tsx           (온보딩 3단계)
│   │   └── (landing)/
│   │       ├── layout.tsx
│   │       └── landing/
│   │           └── page.tsx       (랜딩 페이지)
│   ├── components/
│   │   ├── catalyst/              (27개 원자 컴포넌트)
│   │   │   ├── alert.tsx
│   │   │   ├── avatar.tsx
│   │   │   ├── badge.tsx
│   │   │   ├── button.tsx
│   │   │   ├── checkbox.tsx
│   │   │   ├── combobox.tsx
│   │   │   ├── description-list.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── divider.tsx
│   │   │   ├── dropdown.tsx
│   │   │   ├── fieldset.tsx
│   │   │   ├── heading.tsx
│   │   │   ├── input.tsx
│   │   │   ├── link.tsx
│   │   │   ├── listbox.tsx
│   │   │   ├── navbar.tsx
│   │   │   ├── pagination.tsx
│   │   │   ├── radio.tsx
│   │   │   ├── select.tsx
│   │   │   ├── sidebar-layout.tsx (인앱 미사용)
│   │   │   ├── sidebar.tsx        (인앱 미사용)
│   │   │   ├── stacked-layout.tsx (인앱 미사용)
│   │   │   ├── switch.tsx
│   │   │   ├── table.tsx
│   │   │   ├── text.tsx
│   │   │   └── textarea.tsx
│   │   ├── dotori/                (16개 분자 컴포넌트)
│   │   │   ├── BottomTabBar.tsx
│   │   │   ├── ChatBubble.tsx
│   │   │   ├── StreamingIndicator.tsx
│   │   │   ├── FacilityCard.tsx
│   │   │   ├── ActionCard.tsx
│   │   │   ├── SourceChip.tsx
│   │   │   ├── FilterChip.tsx
│   │   │   ├── ActionConfirmSheet.tsx
│   │   │   ├── Toast.tsx
│   │   │   ├── ToastProvider.tsx
│   │   │   ├── ProgressStepper.tsx
│   │   │   ├── MapEmbed.tsx
│   │   │   ├── CompareTable.tsx
│   │   │   ├── EmptyState.tsx
│   │   │   ├── Skeleton.tsx
│   │   │   └── Wallpaper.tsx
│   │   └── landing/               (Pocket+Salient 기반)
│   │       ├── Hero.tsx
│   │       ├── Header.tsx
│   │       ├── Footer.tsx
│   │       ├── PhoneFrame.tsx
│   │       ├── AppScreen.tsx
│   │       ├── PrimaryFeatures.tsx
│   │       ├── SecondaryFeatures.tsx
│   │       ├── Reviews.tsx
│   │       ├── CallToAction.tsx
│   │       ├── Container.tsx
│   │       ├── Button.tsx
│   │       ├── Logo.tsx
│   │       ├── NavLinks.tsx
│   │       ├── AppStoreLink.tsx
│   │       ├── SalientPricing.tsx
│   │       ├── SalientTestimonials.tsx
│   │       └── FAQ.tsx
│   ├── hooks/
│   │   └── use-app-state.tsx      (Context + Reducer)
│   ├── lib/
│   │   ├── utils.ts               (cn, formatRelativeTime, etc.)
│   │   └── mock-data.ts           (전체 목업 데이터)
│   └── types/
│       └── dotori.ts              (모든 인터페이스)
├── postcss.config.mjs
├── next.config.ts
├── prettier.config.js
├── tsconfig.json
└── package.json
```

---

## 17. 빌드 순서 (6 Phase)

### Phase 0: 인프라 (30분)

```
□ CLI 초기화 (섹션 1)
□ ZIP 추출 + 경로 수정 (섹션 2)
□ postcss.config.mjs, next.config.ts, prettier.config.js (섹션 3)
□ globals.css 테마 (섹션 4)
□ layout.tsx 루트 (섹션 5)
□ manifest.json (섹션 6)
□ types/dotori.ts (섹션 7 — Part 1)
□ lib/utils.ts (섹션 8 — Part 1)
□ lib/mock-data.ts (섹션 9 — Part 1)
□ hooks/use-app-state.tsx (섹션 10 — Part 1)
□ npm run dev → 빈 페이지 확인
```

### Phase 1: 뼈대 (1~2시간)

```
□ (app)/layout.tsx (인앱 레이아웃)
□ BottomTabBar.tsx → 5탭 네비게이션 동작 확인
□ 각 라우트에 임시 <h1> 페이지 생성
□ 탭 이동 + 활성탭 하이라이트 확인
```

### Phase 2: 컴포넌트 제작 (4~6시간)

```
우선순위 순서:
□ SourceChip (다른 컴포넌트에서 많이 사용)
□ FilterChip
□ StreamingIndicator
□ Skeleton
□ EmptyState
□ ProgressStepper
□ ChatBubble
□ FacilityCard (compact + full)
□ ActionCard
□ ActionConfirmSheet
□ Toast + ToastProvider
□ MapEmbed (MVP: 정적)
□ CompareTable
□ Wallpaper (랜딩용)
□ FAQ (랜딩용)
```

### Phase 3: 인앱 화면 조립 (4~6시간)

```
□ 홈 (13-A) — NBA + 변동 + 브리핑
□ 토리챗 (13-B) — 채팅 + 인라인 카드 + 액션 플로우
□ 탐색 (13-C) — 검색 + 필터 + 리스트
□ 이웃 (13-D) — 피드 + EmptyState
□ MY (13-E) — 프로필 + 메뉴
□ 시설상세 (13-F) — 상세정보 + 액션바
□ 온보딩 (13-G) — 3단계 폼
```

### Phase 4: 랜딩 페이지 (2~3시간)

```
□ Pocket 컴포넌트 커스터마이징
□ Salient Pricing/Testimonials 통합
□ Oatmeal Stats 재작성
□ 도토리 FAQ 조립
□ 10개 섹션 조립 확인
```

### Phase 5: 연결 + 마무리 (1~2시간)

```
□ mock-data → 전체 화면에 연결
□ Read→UI→Confirm→Write 플로우 end-to-end 테스트
□ ActionConfirmSheet → Toast 플로우 확인
□ 온보딩 → 홈 전환 확인
□ npm run build → 에러 0 확인
□ 모바일 뷰포트 (375px) 전체 화면 확인
```

---

## 18. ⛔ 절대 하지 말 것 (10가지)

```
1. Oatmeal 컴포넌트 직접 import 금지
   → @tailwindplus/elements는 비공개 패키지
   → Oatmeal 파일은 참고만, 전부 재작성

2. framer-motion 설치 금지
   → motion 사용 (동일 라이브러리, 새 패키지명)

3. Catalyst sidebar/navbar를 인앱에서 사용 금지
   → 데스크톱 SaaS용. 모바일 앱에서는 BottomTabBar 사용

4. 'use client' 남발 금지
   → 서버 컴포넌트가 기본. 상태/이벤트/브라우저API 쓸 때만 클라이언트

5. 영문 Lorem ipsum 금지
   → 모든 텍스트는 한국어. 실제 육아 맥락의 문장 사용

6. Tailwind v3 문법 금지
   → v4 사용: @theme 블록, oklch 컬러, --spacing() 함수

7. 다크모드 구현 금지
   → MVP 범위 밖. 추후 Oatmeal 다크모드 토큰 참고

8. 외부 상태관리 라이브러리 설치 금지
   → Zustand, Redux, Jotai 등 불필요. Context+Reducer로 충분

9. 인앱에서 max-w 제거 금지
   → 반드시 max-w-md mx-auto 유지 (모바일 앱 느낌)

10. Catalyst import 경로를 상대경로로 쓰기 금지
    → 반드시 @/components/catalyst/ 절대경로 사용
    → 섹션 2에서 sed 명령으로 일괄 수정 완료해야 함
```

---

## 19. Oatmeal 재작성 가이드 (교체 매핑)

Oatmeal 코드에서 참고할 패턴과, 그것을 도토리에서 어떻게 재작성하는지:

| Oatmeal 원본 | 의존성 문제 | 도토리 재작성 |
|-------------|-----------|-------------|
| `import { ElDisclosure } from '@tailwindplus/elements/react'` | 비공개 | `import { Disclosure } from '@headlessui/react'` |
| `import { clsx } from 'clsx/lite'` | 미설치 | `import clsx from 'clsx'` |
| `<Section>` (oatmeal 자체 래퍼) | 비공개 | `<section className="py-16 px-6">` |
| `<Subheading>` (oatmeal 자체) | 비공개 | Catalyst의 `<Heading>` 또는 직접 `<h2>` |
| `<Text>` (oatmeal 자체) | 비공개 | Catalyst의 `<Text>` |
| Wallpaper 컴포넌트 | 자체 의존 없음 ✓ | **그대로 가져올 수 있음** (C-15 참고) |
| Stats 패턴 | Section 의존 | 레이아웃만 참고하여 직접 작성 |
| FAQ Accordion | ElDisclosure 의존 | Headless UI Disclosure로 재작성 (C-16) |
| Plan Comparison Table | ElDisclosure 의존 | 직접 작성 또는 Catalyst table 확장 |

---

## 20. V2 → V3 개선 체크리스트

| # | V2 누락/오류 | V3 해결 |
|---|------------|--------|
| 1 | MapEmbed 설계 누락 | ✅ C-11 완전 명세 |
| 2 | CompareTable 설계 누락 | ✅ C-12 완전 명세 |
| 3 | EmptyState 설계 누락 | ✅ C-13 완전 명세 |
| 4 | lib/utils.ts 내용 없음 | ✅ Part 1 섹션 8 |
| 5 | lib/mock-data.ts 내용 없음 | ✅ Part 1 섹션 9 |
| 6 | postcss.config.mjs 없음 | ✅ Part 1 섹션 3-1 |
| 7 | next.config.ts 없음 | ✅ Part 1 섹션 3-2 |
| 8 | Pretendard 폰트 로딩 없음 | ✅ Part 1 섹션 5 (CDN) |
| 9 | facility/[id] 화면 없음 | ✅ 13-F 완전 명세 |
| 10 | Read→UI→Confirm→Write 가이드 없음 | ✅ 섹션 15 완전 명세 |
| 11 | 상태관리 전략 없음 | ✅ Part 1 섹션 10 |
| 12 | 에러/빈상태 처리 없음 | ✅ EmptyState + ActionSheet error 분기 |
| 13 | UI Blocks 활용 가이드 누락 | ✅ Part 2 Catalyst 매핑표 |
| 14 | Connector Layer 미반영 | ✅ 섹션 15 패턴 가이드 |
| 15 | Catalyst import 경로 미확인 | ✅ Part 1 섹션 2-2 (실제 패턴 확인) |
| 16 | Oatmeal 재작성 가이드 없음 | ✅ 섹션 19 교체 매핑 |
| 17 | PWA manifest 없음 | ✅ Part 1 섹션 6 |
| 18 | 반응형 전략 없음 | ✅ Part 1 섹션 11 |

---

## 사용법

```bash
# Claude Code CLI(Ubuntu)에서:

# 1. 3개 파일 확인
cat /path/to/DOTORI_CODEX_V3_PART1_SETUP.md
cat /path/to/DOTORI_CODEX_V3_PART2_COMPONENTS.md
cat /path/to/DOTORI_CODEX_V3_PART3_SCREENS.md

# 2. 프롬프트
"이 3개 DOTORI_CODEX_V3 파일을 순서대로 읽고,
 Part 1의 섹션 1부터 차례로 실행해서 도토리 앱을 처음부터 만들어줘.
 4개 ZIP 파일은 /mnt/user-data/uploads/에 있어:
 - catalyst-ui-kit__2_.zip
 - pocket.zip
 - salient.zip
 - oatmeal-olive-instrument.zip"
```

---

*DOTORI CODEX V3 완료.*
