# 🔧 CODEX 마스터 지시서: 도토리(Dotori) AI 앱 리빌드

> **⚠️ 아카이브 문서** — 이 문서는 초기 설계 명세입니다. 현재 코드와 상당한 괴리가 있습니다.
> **현재 상태 참조:** `.serena/memories/project_overview.md`, `CLAUDE.md`, `docs/ops/MASTER_v1.md`
> **주요 차이:** Next.js 15→16.1, 시설 12K→20,027, API 7→36개, 모델 6→14개, 컴포넌트 16→40+

> **목적**: 기존 도토리 UX/UI를 전면 교체하고, Tailwind Plus 자산(Catalyst UI Kit + Pocket + Salient + Oatmeal)을 기반으로 2026년 에이전틱 AI 앱 표준에 맞는 새로운 프론트엔드를 구축한다.

---

## 0. 프로젝트 개요

| 항목 | 값 |
|------|-----|
| **앱 이름** | 도토리 (Dotori) — 육아맘 AI 입소 전략 앱 |
| **타겟 유저** | 한국 육아맘 (어린이집 입소 준비 중) |
| **프레임워크** | Next.js 15 + React 19 + TypeScript |
| **스타일링** | Tailwind CSS v4 + Headless UI v2.2 |
| **애니메이션** | motion (framer-motion 후속) v12 |
| **아이콘** | @heroicons/react v2.2 |
| **한글 폰트** | Pretendard |
| **영문 폰트** | Inter |

---

## 1. ZIP 파일 자산 맵

Codex가 사용할 4개 ZIP 파일과 각각에서 꺼내 쓸 파일:

### 1-A. Catalyst UI Kit (`catalyst-ui-kit.zip`)

**역할: 인앱 기초 컴포넌트 (Layer 1 — 원자)**

경로: `catalyst-ui-kit/typescript/`

| 컴포넌트 파일 | 도토리 용도 | 우선순위 |
|---|---|---|
| `button.tsx` | 모든 CTA 버튼 (대기신청, 확인, 취소 등) | P0 |
| `dialog.tsx` | 액션 확인 바텀시트 (대기신청 확인, 알림설정 확인) | P0 |
| `alert.tsx` | 경고/에러 모달 (실패 알림, 되돌리기 확인) | P0 |
| `badge.tsx` | 상태 뱃지 (TO있음=green, 대기중=amber, 마감=red, 출처=zinc) | P0 |
| `avatar.tsx` | 사용자 프로필, 시설 아이콘 | P0 |
| `input.tsx` | 토리챗 입력, 검색창, 텍스트 입력 | P0 |
| `checkbox.tsx` | 서류 체크리스트, 동의 체크 | P0 |
| `switch.tsx` | 알림 ON/OFF, 설정 토글 | P0 |
| `select.tsx` | 지역 선택, 시설 유형 필터 | P1 |
| `combobox.tsx` | 시설 검색 자동완성 | P1 |
| `radio.tsx` | 온보딩 선택지 (아이 나이대, 시설 유형 선호) | P1 |
| `dropdown.tsx` | 정렬/필터 드롭다운 | P1 |
| `fieldset.tsx` | 폼 그룹핑 (아이 등록, 설정) | P1 |
| `textarea.tsx` | 후기 작성, 긴 텍스트 입력 | P2 |
| `table.tsx` | 시설 비교 테이블 | P1 |
| `description-list.tsx` | 시설 상세 정보, 액션 확인 프리뷰 | P1 |
| `heading.tsx` | 페이지 제목, 섹션 헤딩 | P0 |
| `text.tsx` | 본문 텍스트 | P0 |
| `link.tsx` | 내비게이션 링크 | P0 |
| `divider.tsx` | 섹션 구분선 | P2 |
| `pagination.tsx` | 시설 목록 페이지네이션 | P2 |
| `listbox.tsx` | 복수 선택 드롭다운 | P2 |
| `navbar.tsx` | ❌ 사용 안함 (모바일 탭바로 대체) |
| `sidebar.tsx` | ❌ 사용 안함 (모바일 앱이므로) |
| `sidebar-layout.tsx` | ❌ 사용 안함 |
| `stacked-layout.tsx` | ❌ 사용 안함 |
| `auth-layout.tsx` | ❌ 사용 안함 (도토리 전용 온보딩으로 대체) |

데모 레이아웃 참고: `catalyst-ui-kit/demo/typescript/src/app/(app)/page.tsx`
→ Stat 카드 + Table 패턴을 도토리 홈 대시보드에 적용

---

### 1-B. Pocket Template (`pocket.zip`)

**역할: 앱스토어 랜딩 페이지 + 모바일 프레임 패턴**

경로: `tailwind-plus-pocket/pocket-ts/src/`

| 컴포넌트 파일 | 도토리 용도 | 변환 방법 |
|---|---|---|
| `components/Hero.tsx` | 랜딩 메인 히어로 | 카피/이미지를 도토리용으로 교체 |
| `components/PhoneFrame.tsx` | 📱 **모바일 앱 프레임** | 랜딩 내 앱 스크린샷 표시용 |
| `components/AppScreen.tsx` | 📱 **모바일 앱 내부 화면** | 랜딩 내 앱 데모 표시용 |
| `components/AppDemo.tsx` | 📱 인터랙티브 앱 데모 | 랜딩 내 기능 시연 |
| `components/Pricing.tsx` | 프리미엄 플랜 가격표 | 도토리 무료/프리미엄 플랜 |
| `components/PrimaryFeatures.tsx` | 핵심 기능 소개 (탭 전환형) | AI 입소전략/토리챗/TO알림 소개 |
| `components/SecondaryFeatures.tsx` | 부가 기능 소개 | 동네추천/시설비교/서류관리 |
| `components/Reviews.tsx` | 사용자 후기 캐러셀 | 육아맘 후기 |
| `components/Faqs.tsx` | FAQ 섹션 | 도토리 자주 묻는 질문 |
| `components/CallToAction.tsx` | CTA 섹션 | 앱 다운로드 유도 |
| `components/AppStoreLink.tsx` | 스토어 링크 버튼 | App Store / Google Play |
| `components/Header.tsx` | 랜딩 헤더 | 도토리 로고 + 네비게이션 |
| `components/Footer.tsx` | 랜딩 푸터 | 회사정보/링크 |
| `components/Button.tsx` | 랜딩 버튼 | Catalyst Button으로 통합 가능 |
| `components/Fields.tsx` | 로그인/가입 폼 | 필요시 사용 |
| `components/Logo.tsx` | ❌ 도토리 로고로 교체 |
| `components/StockLogos.tsx` | ❌ 사용 안함 |
| `components/NavLinks.tsx` | 참고만 (모바일 네비) |

---

### 1-C. Salient Template (`salient.zip`)

**역할: 프라이싱 + 기능 소개 섹션 + 소셜 증거**

경로: `tailwind-plus-salient/salient-ts/src/`

| 컴포넌트 파일 | 도토리 용도 | 비고 |
|---|---|---|
| `components/Pricing.tsx` | ⭐ **가격 비교 테이블** (3단 비교) | Pocket보다 더 상세한 가격표 |
| `components/Testimonials.tsx` | ⭐ **후기 카드 그리드** | 카드형 레이아웃이 더 적합 |
| `components/PrimaryFeatures.tsx` | 스크린샷 기반 기능 소개 | 탭+스크린샷 패턴 참고 |
| `components/SecondaryFeatures.tsx` | 아이콘 기반 기능 소개 | 카드형 패턴 참고 |
| `components/Hero.tsx` | 대안 히어로 (Pocket과 다른 스타일) | 필요시 사용 |
| `components/CallToAction.tsx` | CTA | 배경 이미지 스타일 |
| `components/Faqs.tsx` | FAQ | Pocket과 다른 레이아웃 |
| `components/SlimLayout.tsx` | 인증 페이지 레이아웃 | 로그인/가입 |
| `components/Logo.tsx` | ❌ 도토리 로고로 교체 |

---

### 1-D. Oatmeal Kit (`oatmeal-olive-instrument.zip`)

**역할: 테마 시스템 구조 + 섹션 조각(section primitives)**

⚠️ Oatmeal은 다른 템플릿과 구조가 다름. `components/` + `pages/` 조각 형태.

**핵심 가치: 테마 시스템**

파일: `tailwind.css`
```css
@theme {
  --font-display: 'Instrument Serif', serif;
  --font-sans: 'Inter', system-ui, sans-serif;
  --color-olive-50 ~ 950: oklch(...);
}
```
→ 이 구조를 복사하여 **도토리 테마로 변환**

| 파일 | 도토리 용도 |
|---|---|
| `tailwind.css` | ⭐ **테마 토큰 구조** 그대로 차용 → 도토리 컬러로 교체 |
| `components/elements/wallpaper.tsx` | ⭐ **배경 텍스처** 패턴 (노이즈 + 그라데이션) → 도토리 배경 |
| `components/elements/button.tsx` | 참고 (Catalyst 버튼 사용) |
| `components/elements/announcement-badge.tsx` | ⭐ 공지 뱃지 패턴 → "새로운 기능" 알림 |
| `components/elements/section.tsx` | 섹션 래퍼 구조 참고 |
| `components/elements/screenshot.tsx` | 스크린샷 표시 패턴 |
| `components/sections/pricing-multi-tier.tsx` | 가격 비교 대안 |
| `components/sections/pricing-hero-multi-tier.tsx` | 가격 히어로 |
| `components/sections/plan-comparison-table.tsx` | ⭐ 플랜 비교 상세 테이블 |
| `components/sections/testimonials-three-column-grid.tsx` | 후기 그리드 |
| `components/sections/testimonial-with-large-quote.tsx` | 대표 후기 |
| `components/sections/faqs-accordion.tsx` | ⭐ 아코디언 FAQ |
| `components/sections/faqs-two-column-accordion.tsx` | 2단 FAQ |
| `components/sections/stats-with-graph.tsx` | ⭐ 통계 + 그래프 → 입소 확률 시각화 |
| `components/sections/stats-four-columns.tsx` | 숫자 통계 카드 → "12,000+ 시설" |
| `components/sections/stats-three-column-with-description.tsx` | 설명 포함 통계 |
| `components/sections/hero-centered-with-demo.tsx` | 데모 포함 히어로 |
| `components/sections/features-three-column.tsx` | 3단 기능 카드 |
| `components/sections/features-with-large-demo.tsx` | 대형 데모 기능 소개 |
| `components/sections/navbar-with-logo-actions-and-left-aligned-links.tsx` | 네비바 패턴 |
| `components/sections/footer-with-newsletter-form-categories-and-social-icons.tsx` | 푸터 |
| `pages/home-01.tsx` ~ `home-03.tsx` | ⭐ 전체 페이지 조합 참고 |
| `pages/pricing-01.tsx` ~ `pricing-03.tsx` | 가격 페이지 조합 참고 |

---

## 2. 도토리 테마 토큰

Oatmeal의 `tailwind.css` 구조를 기반으로, 아래 도토리 전용 테마를 적용:

```css
/* dotori-theme.css */
@import 'tailwindcss';

@theme {
  /* Typography */
  --font-display: 'Pretendard', system-ui, sans-serif;
  --font-sans: 'Pretendard', system-ui, sans-serif;
  --font-mono: 'JetBrains Mono', monospace;

  /* 도토리 Primary — Warm Gold/Brown (현재 앱의 따뜻한 톤 유지) */
  --color-dotori-50: oklch(97.5% 0.008 75);
  --color-dotori-100: oklch(94% 0.016 75);
  --color-dotori-200: oklch(88% 0.032 75);
  --color-dotori-300: oklch(80% 0.055 72);
  --color-dotori-400: oklch(70% 0.085 65);
  --color-dotori-500: oklch(60% 0.100 60);   /* 메인 액센트 */
  --color-dotori-600: oklch(50% 0.090 58);
  --color-dotori-700: oklch(42% 0.075 56);
  --color-dotori-800: oklch(33% 0.055 54);
  --color-dotori-900: oklch(25% 0.035 52);
  --color-dotori-950: oklch(17% 0.018 50);

  /* 도토리 Secondary — Forest Green (자연/안전 느낌) */
  --color-forest-50: oklch(97% 0.006 145);
  --color-forest-100: oklch(93% 0.015 145);
  --color-forest-200: oklch(87% 0.030 145);
  --color-forest-300: oklch(78% 0.055 145);
  --color-forest-400: oklch(65% 0.080 145);
  --color-forest-500: oklch(55% 0.090 145);  /* TO 있음, 성공 */
  --color-forest-600: oklch(45% 0.075 145);
  --color-forest-700: oklch(38% 0.060 145);
  --color-forest-800: oklch(30% 0.040 145);
  --color-forest-900: oklch(22% 0.025 145);
  --color-forest-950: oklch(15% 0.012 145);

  /* Semantic Colors */
  --color-success: var(--color-forest-500);
  --color-warning: oklch(75% 0.120 80);       /* amber 계열 — 대기중 */
  --color-danger: oklch(60% 0.180 25);        /* red 계열 — 마감/에러 */
  --color-info: oklch(65% 0.100 240);         /* blue 계열 — 정보 */

  /* Border Radius — 모바일 친화적 둥근 모서리 */
  --radius-sm: 0.5rem;    /* 8px */
  --radius-md: 0.75rem;   /* 12px */
  --radius-lg: 1rem;      /* 16px — 기본 카드 */
  --radius-xl: 1.25rem;   /* 20px — 큰 카드/모달 */
  --radius-2xl: 1.5rem;   /* 24px — 바텀시트 */
  --radius-full: 9999px;  /* 칩/뱃지 */
}

@layer base {
  html {
    -webkit-font-smoothing: antialiased;
    -moz-osx-font-smoothing: grayscale;
    background-color: var(--color-dotori-50);
    font-family: var(--font-sans);
  }
}
```

---

## 3. 도토리 전용 커스텀 컴포넌트 (Layer 2 — 분자)

Catalyst에 없어서 **새로 만들어야** 하는 컴포넌트들.
각 컴포넌트의 구현 규칙:
- Catalyst 패턴을 따른다 (Headless UI + clsx + forwardRef)
- Tailwind v4 유틸리티만 사용
- 모든 컬러는 도토리 테마 토큰 참조
- TypeScript strict 모드

### 3-1. `BottomTabBar.tsx` — 모바일 5탭 내비게이션

```
역할: 홈 / 탐색 / 토리챗 / 이웃 / MY 탭 전환
구조:
- fixed bottom-0, safe-area-inset 대응
- 5개 탭 아이콘 + 라벨
- 현재 활성 탭 하이라이트 (dotori-500 색상)
- 토리챗 탭은 중앙에 크게 (FAB 스타일)
참고: heroicons의 HomeIcon, MagnifyingGlassIcon, ChatBubbleOvalLeftIcon, MapPinIcon, UserCircleIcon 사용
```

### 3-2. `ChatBubble.tsx` — 채팅 메시지 버블

```
역할: 토리챗의 사용자/AI 메시지 표시
Props:
- role: 'user' | 'assistant'
- children: ReactNode (텍스트, 카드, 지도 등 임베드)
- timestamp: string
- sources?: SourceChip[] (AI 메시지에만)
구조:
- user: 오른쪽 정렬, dotori-500 배경, white 텍스트
- assistant: 왼쪽 정렬, white/dotori-50 배경, 좌상단 도토리 아바타
- 카드/위젯이 children으로 들어올 수 있어야 함
```

### 3-3. `StreamingIndicator.tsx` — AI 응답 중 표시

```
역할: 토리챗에서 AI 응답 생성 중 표시
구조:
- 3개 점 애니메이션 (motion 라이브러리 사용)
- ChatBubble assistant 스타일 내부에 렌더링
- "토리가 분석 중이에요..." 텍스트 옵션
```

### 3-4. `FacilityCard.tsx` — 시설 정보 카드

```
역할: 어린이집 정보를 카드 형태로 표시
Props:
- name: string
- type: '국공립' | '민간' | '가정' | '직장'
- distance: string
- capacity: { total: number, current: number, waiting: number }
- status: 'available' | 'waiting' | 'full'
- lastUpdated: string
- onAction?: () => void
구조:
- 상단: 시설명 + 유형 Badge(Catalyst)
- 중단: 정원/현원/대기 숫자 (Description List 패턴)
- 하단: SourceChip(갱신시각) + 액션 버튼(관심등록/대기신청)
- status에 따른 색상: available=forest-500, waiting=warning, full=danger
```

### 3-5. `ActionCard.tsx` — CTA 포함 액션 카드

```
역할: "다음 행동" 제안 카드 (NBA: Next Best Action)
Props:
- icon: ReactNode
- title: string
- description: string
- actionLabel: string
- onAction: () => void
- priority: 'high' | 'normal'
구조:
- 좌측 아이콘 + 우측 텍스트 + 하단 CTA 버튼
- priority=high일 때 좌측 dotori-500 보더 강조
- 홈 상단, 토리챗 상단에 1~2개 자동 표시
```

### 3-6. `SourceChip.tsx` — 데이터 출처 표시

```
역할: AI 결과의 데이터 소스/갱신시각 표시
Props:
- source: string ('아이사랑' | '지도' | '후기' | '정부24')
- updatedAt: string | Date
- freshness: 'realtime' | 'recent' | 'cached'
구조:
- Catalyst Badge를 확장
- freshness에 따른 색상: realtime=forest, recent=amber, cached=zinc
- 클릭 시 상세 팝오버 (데이터 범위, 마지막 갱신, 커버리지)
- 텍스트: "아이사랑 · 2분전"
```

### 3-7. `FilterChip.tsx` — 선택형 필터 태그

```
역할: 시설 유형, 지역 등 필터 다중선택
Props:
- label: string
- selected: boolean
- onToggle: () => void
구조:
- 선택 안됨: outline 스타일 (dotori-200 border)
- 선택됨: solid 스타일 (dotori-500 background, white text)
- 토리챗 상단 프롬프트 칩으로도 사용 ("동네추천" "입소전략" "시설비교")
```

### 3-8. `ActionConfirmSheet.tsx` — 실행 확인 바텀시트

```
역할: Write Action 실행 전 확인 (대기신청, 알림설정 등)
구조: Catalyst Dialog를 확장
- 상단: 제목 + 아이콘
- 중단: DescriptionList로 프리뷰 (아이 이름, 시설명, 대기번호 등)
- 체크박스: "위 내용이 맞습니다" 동의
- 하단: "취소" (outline) + "확인" (solid dotori-500)
- 실행 중: 버튼이 로딩 스피너로 변환
- 완료: 성공/실패 아이콘 + 메시지 + "확인" 버튼
```

### 3-9. `Toast.tsx` — 성공/실패/되돌리기 알림

```
역할: 액션 실행 결과를 하단에 일시적으로 표시
Props:
- type: 'success' | 'error' | 'info' | 'undo'
- message: string
- action?: { label: string, onClick: () => void } (되돌리기 등)
- duration?: number (기본 4000ms)
구조:
- fixed bottom, safe-area 위, tab bar 위
- 좌측 아이콘 + 메시지 + 우측 액션 버튼
- 자동 dismiss 애니메이션 (motion)
```

### 3-10. `ProgressStepper.tsx` — 온보딩/체크리스트 진행

```
역할: 아이 등록 온보딩 (3단계), 서류 체크리스트 진행
Props:
- steps: { label: string, status: 'done' | 'current' | 'upcoming' }[]
- currentStep: number
구조:
- 가로 스텝 인디케이터 (원형 + 라인)
- done=forest-500 체크, current=dotori-500 채움, upcoming=zinc-300 빈원
- 각 스텝 라벨 표시
```

### 3-11. `MapEmbed.tsx` — 인라인 지도 위젯

```
역할: 토리챗/시설상세 내 지도 표시
Props:
- markers: { lat: number, lng: number, label: string, status: string }[]
- center: { lat: number, lng: number }
- height: string (기본 '200px')
구조:
- Kakao Map 또는 Naver Map API 연동
- 커스텀 마커 (시설 상태 색상)
- ChatBubble 안에 임베드 가능한 크기
```

### 3-12. `CompareTable.tsx` — 시설 비교 (경량 테이블)

```
역할: 2~3개 시설 나란히 비교
Props:
- facilities: FacilityData[]
- criteria: string[] (비교 항목)
구조:
- Catalyst Table을 경량화
- 가로 스크롤 가능 (모바일)
- 최상위 행: 시설명 + 유형 Badge
- 비교 항목별 셀에 값 + 하이라이트 (최적값)
```

### 3-13. `Skeleton.tsx` — 로딩 플레이스홀더

```
역할: 데이터 로딩 중 골격 UI
Props:
- variant: 'card' | 'list' | 'chat' | 'text'
구조:
- animate-pulse 기반
- 각 variant에 맞는 골격 형태
- dotori-100 배경 + dotori-200 골격 블록
```

---

## 4. 화면별 조립 지시서

### 4-A. 홈 화면 (개인화 대시보드)

```
레이아웃: BottomTabBar 포함 전체 화면
구성:
┌─────────────────────────┐
│ 헤더: "○○맘, 좋은 아침이에요" │  ← Catalyst Heading
│ + 알림 아이콘               │  ← heroicons BellIcon
├─────────────────────────┤
│ NBA 카드 1~2개             │  ← ActionCard (P0)
│ "아이 등록하면 입소확률 분석 가능" │
├─────────────────────────┤
│ 오늘의 변동 (상태 카드)      │  ← FacilityCard 축약 버전
│ "관심시설 A: 대기 12→9번"    │
│ "관심시설 B: TO 1명 발생!"   │
├─────────────────────────┤
│ 통계 요약 (2x2 그리드)      │  ← Catalyst Demo의 Stat 패턴
│ 전국시설 12,000+ | AI 가동중  │     + Badge
│ TO 감지 24h    | 이용료 무료  │
├─────────────────────────┤
│ AI 브리핑 카드              │  ← ActionCard
│ "매일 아침 AI 브리핑 시작하기"  │
├─────────────────────────┤
│       BottomTabBar        │
└─────────────────────────┘

데이터 출처: 모든 AI 결과 카드에 SourceChip 포함
참고 템플릿:
- 통계 그리드 → catalyst-ui-kit/demo/typescript/src/app/(app)/page.tsx (Stat 컴포넌트)
- 숫자 카드 → Oatmeal components/sections/stats-four-columns.tsx
```

### 4-B. 토리챗

```
레이아웃: 전체 화면, 하단 입력창 고정
구성:
┌─────────────────────────┐
│ 헤더: "토리" + Streaming 표시 │ ← 🔵 실시간 | ✨ Streaming...
├─────────────────────────┤
│ 프롬프트 칩 (가로 스크롤)     │ ← FilterChip
│ [동네추천] [입소전략] [시설비교] │   [7일체크]
├─────────────────────────┤
│                          │
│ ChatBubble (assistant)    │ ← AI 인사 메시지
│  "반가워요! 궁금한 걸..."    │
│                          │
│ 추천 카드 3개              │ ← ActionCard (동네추천, 입소전략, 시설비교)
│  각각 아이콘 + 1줄 설명 + →  │
│                          │
│ ... 대화 진행 ...           │
│                          │
│ ChatBubble (assistant)    │
│  "○○동 근처 3곳 찾았어요"   │
│  ┌── FacilityCard ──┐    │ ← 인라인 시설 카드
│  │ 해피어린이집 TO:2  │    │
│  └──────────────────┘    │
│  ┌── MapEmbed ──────┐    │ ← 인라인 지도
│  │    📍 📍 📍       │    │
│  └──────────────────┘    │
│  SourceChip: 아이사랑·2분전  │ ← 출처 표시
│  [비교하기] [관심등록] [대기신청]│ ← Catalyst Button group
│                          │
├─────────────────────────┤
│ 💬 토리에게 물어보세요   [↑] │ ← Catalyst Input + Button
├─────────────────────────┤
│       BottomTabBar        │
└─────────────────────────┘

핵심: Write Action 흐름
사용자: "해피어린이집 대기 신청해줘"
→ ActionConfirmSheet 열림 (Dialog 확장)
  - 프리뷰: 아이 이름, 생년월일, 시설명 (DescriptionList)
  - 체크박스: 동의 (Checkbox)
  - 버튼: 취소/확인 (Button)
→ 실행 중: 버튼 로딩 상태
→ 완료: Toast "대기 신청 완료 · 되돌리기"
```

### 4-C. 탐색 (도구함)

```
레이아웃: BottomTabBar 포함
구성:
┌─────────────────────────┐
│ 검색창: "앱 검색"           │ ← Catalyst Input (search icon)
│ 도구함 / 오늘 할 일         │ ← 탭 전환 (FilterChip)
├─────────────────────────┤
│ 카테고리 칩 (가로 스크롤)    │ ← FilterChip
│ [추천] [부동산] [금융] [쇼핑] │
├─────────────────────────┤
│ 2x2 도구 타일 그리드        │
│ ┌──────┐ ┌──────┐       │
│ │지도연결│ │입소전략│       │ ← ActionCard 변형 (아이콘 + 라벨)
│ │ Badge │ │ Badge │       │    Badge로 "지도" "AI" 등 표시
│ └──────┘ └──────┘       │
│ ┌──────┐ ┌──────┐       │
│ │TO모니터│ │연장가드│       │
│ │ Badge │ │ Badge │       │
│ └──────┘ └──────┘       │
├─────────────────────────┤
│ 토리에게 직접 물어보기       │ ← 큰 CTA → 토리챗으로 딥링크
├─────────────────────────┤
│       BottomTabBar        │
└─────────────────────────┘
```

### 4-D. 이웃 (커뮤니티)

```
레이아웃: BottomTabBar 포함
구성:
┌─────────────────────────┐
│ 이웃 / 전체     [글쓰기]  │ ← Catalyst Heading + Button
│ [GPS 인증으로 동네 글 확인] │ ← Alert Banner (announcement-badge 패턴)
├─────────────────────────┤
│ 탭: [최신] [인기] [질문] [후기] │ ← FilterChip
├─────────────────────────┤
│ (게시글이 없을 때)          │
│ AI 생성 시드 콘텐츠:        │
│ "이 동네 입소 트렌드 리포트"  │ ← ActionCard
│ SourceChip: AI분석·오늘     │
├─────────────────────────┤
│ (게시글이 있을 때)          │
│ 피드 카드:                 │
│ - Avatar + 닉네임 + 시간    │ ← Catalyst Avatar + Text
│ - 본문 텍스트              │
│ - 관련 시설 태그 (Badge)    │
│ - AI 요약 (접을 수 있음)    │
│ - 좋아요/댓글/관심등록 액션  │
├─────────────────────────┤
│       BottomTabBar        │
└─────────────────────────┘
```

### 4-E. MY (마이페이지)

```
레이아웃: BottomTabBar 포함
구성:
┌─────────────────────────┐
│ MY / 사용자님              │ ← Catalyst Heading + Avatar
│ [토리에게 물어보기]          │ ← 링크 → 토리챗
├─────────────────────────┤
│ 2x2 요약 카드              │
│ 알림 0건 | 관심시설 0개     │ ← Badge 포함
│ AI 상담  | 플랜: 무료       │
├─────────────────────────┤
│ 내 아이 섹션               │
│ ┌──────────────────────┐│
│ │ (아이 미등록시)         ││
│ │ 아이를 등록하면 맞춤    ││
│ │ 입소 전략을 받을 수 있어요││ ← ActionCard (priority=high)
│ │ [아이 등록하기] 버튼     ││ ← Catalyst Button (solid dotori)
│ └──────────────────────┘│
├─────────────────────────┤
│ 메뉴 리스트               │ ← Catalyst 패턴 (리스트+아이콘+화살표)
│ 🔔 알림                  │
│ ⚙️ 앱 설정                │
│ 🛡️ 플랜 및 보안           │
├─────────────────────────┤
│       BottomTabBar        │
└─────────────────────────┘

참고: catalyst-ui-kit/demo/typescript/src/app/(app)/settings/page.tsx
```

### 4-F. 온보딩 (첫 실행)

```
레이아웃: 전체 화면 (탭바 없음)
구성 (3단계):

Step 1: 아이 정보
┌─────────────────────────┐
│ ProgressStepper: ●──○──○  │
│ "아이 정보를 알려주세요"    │ ← Catalyst Heading
│                          │
│ 아이 이름: [___________]  │ ← Catalyst Input
│ 생년월일: [____년 __월]   │ ← Catalyst Select x2
│ 성별: ○ 남 ○ 여 ○ 무관   │ ← Catalyst Radio
│                          │
│           [다음 →]        │ ← Catalyst Button
└─────────────────────────┘

Step 2: 지역 설정
┌─────────────────────────┐
│ ProgressStepper: ✓──●──○  │
│ "어디에 사시나요?"         │
│                          │
│ 시/도: [서울특별시 ▼]      │ ← Catalyst Select
│ 시/군/구: [강남구 ▼]      │ ← Catalyst Select
│ 동/읍/면: [역삼동 ▼]      │ ← Catalyst Combobox (자동완성)
│                          │
│ 또는 [현재 위치 사용 📍]   │ ← Catalyst Button (outline)
│           [다음 →]        │
└─────────────────────────┘

Step 3: 관심 시설 유형
┌─────────────────────────┐
│ ProgressStepper: ✓──✓──●  │
│ "어떤 시설을 찾으세요?"    │
│                          │
│ [국공립] [민간] [가정]     │ ← FilterChip (multi-select)
│ [직장] [협동]             │
│                          │
│ 특별히 중요한 것 (선택):    │
│ □ 통학버스 □ 연장보육       │ ← Catalyst Checkbox
│ □ 급식 자체조리 □ CCTV     │
│                          │
│     [도토리 시작하기 🌰]   │ ← Catalyst Button (solid dotori)
└─────────────────────────┘
```

### 4-G. 랜딩 페이지 (앱스토어 마케팅)

```
별도 라우트: /landing 또는 별도 도메인

조립 순서 (위→아래):
1. Header       ← Pocket Header.tsx + 도토리 로고
2. Hero         ← Pocket Hero.tsx + PhoneFrame (도토리 앱 스크린)
3. Stats        ← Oatmeal stats-four-columns.tsx ("12,000+ 시설" "93.8% 시간 절감")
4. Features     ← Pocket PrimaryFeatures.tsx (탭 전환형: 토리챗/입소전략/TO알림)
5. Features2    ← Pocket SecondaryFeatures.tsx (카드형: 동네추천/시설비교/서류관리)
6. Testimonials ← Salient Testimonials.tsx (육아맘 후기 카드 그리드)
7. Pricing      ← Salient Pricing.tsx (무료 vs 프리미엄)
8. FAQ          ← Oatmeal faqs-accordion.tsx
9. CTA          ← Pocket CallToAction.tsx + AppStoreLink.tsx
10. Footer      ← Oatmeal footer-with-links-and-social-icons.tsx
```

---

## 5. 디렉토리 구조

```
dotori-app/
├── src/
│   ├── app/
│   │   ├── (app)/                    # 인앱 (BottomTabBar 포함)
│   │   │   ├── layout.tsx            # 탭바 레이아웃
│   │   │   ├── page.tsx              # 홈 대시보드
│   │   │   ├── explore/page.tsx      # 탐색/도구함
│   │   │   ├── chat/page.tsx         # 토리챗
│   │   │   ├── community/page.tsx    # 이웃
│   │   │   ├── my/page.tsx           # 마이페이지
│   │   │   ├── my/settings/page.tsx  # 설정
│   │   │   └── facility/[id]/page.tsx # 시설 상세
│   │   ├── (onboarding)/             # 온보딩 (탭바 없음)
│   │   │   ├── layout.tsx
│   │   │   └── page.tsx
│   │   ├── (landing)/                # 랜딩 페이지
│   │   │   ├── layout.tsx
│   │   │   └── page.tsx
│   │   ├── layout.tsx                # 루트 레이아웃 (폰트, 테마)
│   │   └── globals.css               # 도토리 테마 (Section 2)
│   │
│   ├── components/
│   │   ├── catalyst/                 # Catalyst 원자 (ZIP에서 복사)
│   │   │   ├── button.tsx
│   │   │   ├── dialog.tsx
│   │   │   ├── alert.tsx
│   │   │   ├── badge.tsx
│   │   │   ├── avatar.tsx
│   │   │   ├── input.tsx
│   │   │   ├── checkbox.tsx
│   │   │   ├── switch.tsx
│   │   │   ├── select.tsx
│   │   │   ├── combobox.tsx
│   │   │   ├── radio.tsx
│   │   │   ├── dropdown.tsx
│   │   │   ├── fieldset.tsx
│   │   │   ├── textarea.tsx
│   │   │   ├── table.tsx
│   │   │   ├── description-list.tsx
│   │   │   ├── heading.tsx
│   │   │   ├── text.tsx
│   │   │   ├── link.tsx
│   │   │   ├── divider.tsx
│   │   │   ├── pagination.tsx
│   │   │   └── listbox.tsx
│   │   │
│   │   ├── dotori/                   # 도토리 분자 (새로 제작)
│   │   │   ├── BottomTabBar.tsx
│   │   │   ├── ChatBubble.tsx
│   │   │   ├── StreamingIndicator.tsx
│   │   │   ├── FacilityCard.tsx
│   │   │   ├── ActionCard.tsx
│   │   │   ├── SourceChip.tsx
│   │   │   ├── FilterChip.tsx
│   │   │   ├── ActionConfirmSheet.tsx
│   │   │   ├── Toast.tsx
│   │   │   ├── ProgressStepper.tsx
│   │   │   ├── MapEmbed.tsx
│   │   │   ├── CompareTable.tsx
│   │   │   └── Skeleton.tsx
│   │   │
│   │   └── landing/                  # 랜딩 컴포넌트 (템플릿에서 커스텀)
│   │       ├── Hero.tsx              # ← Pocket Hero 커스텀
│   │       ├── PhoneFrame.tsx        # ← Pocket PhoneFrame
│   │       ├── Features.tsx          # ← Pocket PrimaryFeatures 커스텀
│   │       ├── Pricing.tsx           # ← Salient Pricing 커스텀
│   │       ├── Testimonials.tsx      # ← Salient Testimonials 커스텀
│   │       ├── Stats.tsx             # ← Oatmeal stats 커스텀
│   │       ├── FAQ.tsx               # ← Oatmeal faqs-accordion 커스텀
│   │       ├── Header.tsx            # ← Pocket Header 커스텀
│   │       └── Footer.tsx            # ← Oatmeal footer 커스텀
│   │
│   └── styles/
│       └── globals.css               # 도토리 테마 토큰 (Section 2)
│
├── package.json
├── tailwind.css                      # @import + @theme
├── postcss.config.mjs
├── next.config.ts
└── tsconfig.json
```

---

## 6. 핵심 실행 규칙

### 6-1. 기존 UX 완전 삭제
- 기존 도토리 앱의 모든 UI 코드를 삭제한다
- 새로운 디자인 시스템은 오직 Catalyst + 도토리 커스텀 컴포넌트만 사용
- 기존 스타일시트, 컴포넌트, 레이아웃 파일 전부 교체

### 6-2. Catalyst 컴포넌트 사용 규칙
- `catalyst-ui-kit/typescript/` 디렉토리의 파일을 `src/components/catalyst/`로 복사
- 컬러만 도토리 테마 토큰으로 오버라이드 (CSS 변수 레벨)
- 컴포넌트 구조/로직은 원본 유지 (Headless UI 패턴)

### 6-3. 반응형 규칙
- **Mobile-first**: 기본 뷰포트는 375px (iPhone SE 기준)
- 모든 컴포넌트는 모바일 최적화 우선
- sm(640px) 이상에서 태블릿 대응
- lg(1024px) 이상은 랜딩 페이지에만 적용

### 6-4. 접근성 규칙
- Headless UI가 보장하는 a11y를 그대로 유지
- 모든 인터랙티브 요소에 aria-label 한국어로 제공
- 색상만으로 정보를 전달하지 않음 (아이콘+텍스트 병행)

### 6-5. 성능 규칙
- 이미지: next/image 사용, WebP 우선
- 폰트: Pretendard subset (한글 사용 글자만)
- 번들: 각 페이지별 코드 스플리팅
- 'use client'는 인터랙션 필요한 컴포넌트에만

---

## 7. 우선순위 실행 순서

### Phase 1: 기초 (1~2일)
1. Next.js 15 프로젝트 초기화
2. Catalyst 컴포넌트 복사 + 도토리 테마 적용
3. BottomTabBar + 기본 라우팅

### Phase 2: 핵심 화면 (3~5일)
4. 홈 대시보드 (Stat + ActionCard + FacilityCard)
5. 토리챗 (ChatBubble + StreamingIndicator + FilterChip)
6. 온보딩 (ProgressStepper + 폼 컴포넌트)

### Phase 3: Write Action (2~3일)
7. ActionConfirmSheet (대기신청 확인 플로우)
8. Toast (성공/실패 알림)
9. SourceChip (모든 AI 카드에 출처 표시)

### Phase 4: 나머지 화면 (2~3일)
10. 탐색/도구함
11. 이웃 커뮤니티
12. MY/설정

### Phase 5: 랜딩 페이지 (1~2일)
13. Pocket/Salient/Oatmeal 조합으로 랜딩 구성

---

*이 문서는 Codex가 도토리 앱을 처음부터 다시 만들 때의 마스터 가이드입니다.*
*모든 디자인 결정은 이 문서를 따르고, 애매한 경우 2026년 에이전틱 AI 앱 UX 원칙을 우선합니다.*
