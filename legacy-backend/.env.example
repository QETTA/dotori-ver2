# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
# 입소ai Environment Variables
# Copy to .env.local and fill in values
# ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

# ── Database ──
DATABASE_URL=mongodb://localhost:27017/ipsoai?directConnection=true
# Optional alias: if DATABASE_URL is empty, app falls back to MONGODB_URI
# MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# ── Auth (NextAuth v5) ──
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your_random_secret_at_least_32_chars

# ── Kakao OAuth ──
KAKAO_CLIENT_ID=your_kakao_rest_api_key
KAKAO_CLIENT_SECRET=your_kakao_client_secret
NEXT_PUBLIC_KAKAO_KEY=your_kakao_javascript_key
NEXT_PUBLIC_KAKAO_MAP_KEY=your_kakao_map_javascript_key
# E2E에서 외부 SDK 호출 차단(실서비스/로컬 기본은 0)
NEXT_PUBLIC_E2E_DISABLE_EXTERNAL_SDK=0

# ── Naver OAuth ──
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# ── Payment (Toss) ──
TOSS_SECRET_KEY=test_sk_xxxxxxxx
NEXT_PUBLIC_TOSS_CLIENT_KEY=test_ck_xxxxxxxx

# ── AI (OpenAI) ──
OPENAI_API_KEY=sk-xxxxxxxx
# Empty = mock streaming mode
AI_PROVIDER=auto            # auto | openai | spark | mock
AI_MODEL=gpt-4o-mini        # 기본 모델(공급사 자동 선택 시 openai용 기본값)
OPENAI_MODEL=gpt-4o-mini    # openai 직접 지정
OPENAI_API_URL=https://api.openai.com/v1
OPENAI_TEMPERATURE=0.55
OPENAI_TOP_P=0.95
OPENAI_MAX_TOKENS=1200
OPENAI_REQUEST_TIMEOUT_MS=30000
# OPENAI_EXTRA_BODY={"reasoning": {"enabled": true}}   # OpenAI 호환 확장 파라미터 (JSON)
SPARK_API_KEY=your_spark_api_key_here
SPARK_API_URL=https://api.spark.local/v1    # Spark 호환 엔드포인트 또는 프록시
SPARK_MODEL=spark-5.3
SPARK_TEMPERATURE=0.35
SPARK_TOP_P=0.9
SPARK_MAX_TOKENS=1800
SPARK_REASONING=true
SPARK_REQUEST_TIMEOUT_MS=30000
# SPARK_EXTRA_BODY={"reasoning": {"enabled": true}, "metadata": {"mode":"reasoning"}} # Spark 호환 확장 파라미터
SPARK_SYSTEM_PROMPT=당신은 어린이집 입소를 도와주는 상담 AI "입소ai"입니다. 존댓말로 간결하고 실용적으로 답변하고, 과장된 보장이나 불확실한 수치 단정을 피하세요. 가능하면 입소 확률은 근거형 지표(대기현황, 경쟁률, 정원 여유, 가점 요건) 중심으로 설명하세요.

# ── Public Data (공공데이터포털) ──
DATA_GO_KR_KEY=your_data_go_kr_api_key

# ── Cron ──
CRON_SECRET=your_random_cron_secret

# ── Optional Services ──
REDIS_URL=redis://localhost:6379
# REDIS_TOKEN=your_upstash_token (if using Upstash)

# ── Analytics & Monitoring ──
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX
# NEXT_PUBLIC_SENTRY_DSN=https://xxx@sentry.io/xxx

# ── App ──
NEXT_PUBLIC_APP_URL=http://localhost:3000
NODE_ENV=development
